class BookModel {
  String title;
  String id;
  String url;
  bool selected;

  BookModel(
      {required this.title,
      required this.url,
      required this.id,
      required this.selected});
}
